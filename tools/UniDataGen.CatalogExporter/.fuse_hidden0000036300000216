// DataGen.CatalogExporter has been removed. The catalog now ships as embedded JSON in DataGen.Configuration
// (industries.json, source-types.json, storage.json, entities.json, attributes.json.gz). See CatalogStore.
// There is no longer a workbook to export from. Edit the JSON resources directly to change the catalog.
System.Console.WriteLine("DataGen.CatalogExporter has been removed; the catalog ships as embedded JSON in DataGen.Configuration.");
